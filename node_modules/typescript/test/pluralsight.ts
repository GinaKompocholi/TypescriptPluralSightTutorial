/* Arrow Functions */

function sumFunction(x:number, y: number) {
    console.log(x + y);
}

sumFunction(10, 20); //returns 30

let sum = (x: number, y: number): void => {
    console.log(x + y);
}

sum(10, 20); //returns 30