"use strict";
/* Arrow Functions */
function sumFunction(x, y) {
    console.log(x + y);
}
sumFunction(10, 20); //returns 30
let sum = (x, y) => {
    console.log(x + y);
};
sum(10, 20); //returns 30
